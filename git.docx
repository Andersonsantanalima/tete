*** Git config ****

Configurando conta do usuário:
  git config --global user.email "n353347@gmail.com"
  git config --global user.name "andersonsantanalima"

Enviar projeto para um repositório
	git remote add origin 
	git branch -M master
	git push -u origin master

Adicionar o que foi feito para o repositório local
	git add .

Enviar o repositório local para o Github
	git push

Criar branch
	git branch nome_branch
	
Mudar de branch
	git checkout nome_branch

Resetar diretório 
	git add .
	git reset --hard HEAD
	git pull
	git checkout 
…or create a new repository on the command line
echo "# t" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/Andersonsantanalima/t.git
git push -u origin main

…or push an existing repository from the command line
git remote add origin https://github.com/Andersonsantanalima/t.git
git branch -M main
git push -u origin main
